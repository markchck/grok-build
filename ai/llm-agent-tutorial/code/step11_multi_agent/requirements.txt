# 이 단계에서 실제로 쓰는 패키지만 나열한다. 버전은 범위로 쓴다(PROJECT-SPEC.md 10절).
# 이전 단계에 이미 있던 패키지(openai, fastapi, uvicorn, python-dotenv, pydantic, httpx)는
# 같은 범위를 그대로 쓴다.

openai>=3.10,<4                     # OpenAI 호환 SDK 호출(docagent/llm.py, 7단계와 동일)

fastapi>=0.115,<1.0                  # 웹 프레임워크(협업 SSE 엔드포인트 + A2A 최소 흉내 서버)
uvicorn[standard]>=0.30,<1.0         # ASGI 서버
python-dotenv>=1.0,<2                # .env 파일에서 환경 변수 로딩
pydantic>=2.7,<3                     # 요청 바디 검증
httpx>=0.28,<0.29                    # a2a_min/client.py가 외부 검증 에이전트를 부를 때 씀

# 11단계에서 새로 추가. 버전은 이 장 작성 시점(2026-09-09) 실측 기준선이다 —
# 설치해서 `pip list`로 직접 확인했다(장 하단 "참고 문서"). 이 장의 코드는
# StateGraph/START/END/add_conditional_edges와 langgraph.types.Command(goto=...)만
# 쓴다 — langgraph-supervisor·langgraph-swarm은 설치해 API를 확인했지만(2-2절)
# 이 장의 코드는 의존하지 않는다.
langgraph>=1.2,<2                    # State/Node/Edge 그래프, Command(goto=...) 기반 핸드오프

pytest>=8.0,<9                       # 단위 테스트 실행용
