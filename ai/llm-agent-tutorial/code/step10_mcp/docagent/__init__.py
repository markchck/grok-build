"""docagent — 10단계 MCP 클라이언트/호스트 쪽 애플리케이션 패키지.

이 패키지가 MCP의 **Host**다. FastAPI 서버(``app.py``)를 띄우는 것도,
MCP 서버 자식 프로세스를 실행하는 것도, 세션이 끊기면 재연결을 시도하는
것도 전부 이 패키지의 책임이다. 실제 도구 함수는 이 패키지 어디에도 없다
— ``../mcp_server/``에 있고, 이 패키지는 그 이름조차 import하지 않는다.
"""
