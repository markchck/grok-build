"""docagent 문서·CSV MCP 서버.

이 패키지는 ``docagent`` 패키지(호스트/클라이언트 쪽 에이전트)와 **의도적으로
분리된** 별도 파이썬 패키지다. 실행할 때도 별도 프로세스(``python -m
mcp_server.server``)로 뜬다 — MCP의 핵심이 "도구를 서버로 분리하는 것"이기
때문에, 코드도 프로세스도 실제로 분리해 둔다.

이 패키지는 ``docagent`` 쪽의 어떤 모듈도 import하지 않는다(반대 방향도
마찬가지). 두 프로세스가 통신하는 유일한 통로는 MCP 프로토콜(표준입출력
또는 HTTP)이며, 같은 파이썬 프로세스 안에서 함수를 직접 호출하는 통로는
없다 — 3단계의 로컬 함수 도구와 가장 크게 다른 지점이다.
"""
