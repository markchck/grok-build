[pytest]
; MCP 클라이언트 코드가 async def이므로 이 장부터 pytest-asyncio가 필요하다.
; auto 모드: async def 테스트 함수를 별도 @pytest.mark.asyncio 데코레이터 없이도
; 인식한다(마커를 붙여도 무해하다 — tests/test_mcp_client_failures.py는 명시적으로 붙여 뒀다).
asyncio_mode = auto
