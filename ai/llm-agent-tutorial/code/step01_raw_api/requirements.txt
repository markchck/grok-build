# 1단계에서 실제로 쓰는 패키지만 담는다.
# 버전은 이 문서(2026-09-09) 작성 시점 기준으로 확인한 값이다. 상위 호환을 단정하지 않는다.

httpx>=0.28.1,<0.29        # raw HTTP 호출용
openai>=3.10.0,<4.0        # OpenAI 호환 SDK
python-dotenv>=1.0.1,<2.0  # .env 파일에서 환경 변수 로딩
