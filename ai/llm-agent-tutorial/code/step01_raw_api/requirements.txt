# 1단계에서 실제로 쓰는 패키지만 담는다.
# 버전은 범위로 쓴다(PROJECT-SPEC.md 10절). 확인된 기준선(2026-09-09 실측):
# openai>=3.10,<4는 httpx가 아니라 httpx2(>=2.7.0,<3)에 의존한다 — 이름이 달라
# httpx를 별도로 명시해도 충돌하지 않는다.

httpx>=0.28,<0.29          # raw HTTP 호출용 (docagent/llm.py의 raw_* 함수)
openai>=3.10,<4            # OpenAI 호환 SDK (docagent/llm.py의 chat/chat_stream/chat_json)
python-dotenv>=1.0,<2      # .env 파일에서 환경 변수 로딩
