# 13단계: 8단계(HITL)와 5단계(하이브리드 검색) 의존성을 합친 것뿐이다.
# 이 장이 새로 추가하는 패키지는 없다 — CSV 생성은 표준 라이브러리 csv/io만 쓴다.
fastapi>=0.115,<1.0          # 서버, 라우팅, StreamingResponse
uvicorn[standard]>=0.30,<1.0 # ASGI 서버
pydantic>=2.7,<3             # 요청 검증, 도구 인자 스키마
httpx>=0.28,<0.29            # 모델 서버(OpenAI 호환 REST) 호출 (llm.py)
python-dotenv>=1.0,<2        # .env 로딩
pymilvus[milvus-lite]>=2.6,<3  # 5단계 하이브리드 검색 재사용 (rag/store.py, rag/search.py)
