# 2단계에서 실제로 쓰는 패키지만 담는다.
# 버전은 이 문서(2026-09-09) 작성 시점 기준으로 확인한 값이다. 상위 호환을 단정하지 않는다.

fastapi>=0.141.0,<0.142        # 웹 프레임워크
uvicorn[standard]>=0.52.0,<0.53  # ASGI 서버 (--reload, 표준 확장 포함)
openai>=3.10.0,<4.0            # OpenAI 호환 SDK (1단계와 동일 버전대)
python-dotenv>=1.2.0,<2.0      # .env 파일에서 환경 변수 로딩
