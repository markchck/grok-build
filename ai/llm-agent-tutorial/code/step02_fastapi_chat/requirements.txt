# 2단계에서 실제로 쓰는 패키지만 담는다.
# 버전은 범위로 쓴다(PROJECT-SPEC.md 10절 기준선, 2026-09-09 실측).
# openai와 python-dotenv는 1단계와 같은 범위를 그대로 쓴다.

fastapi>=0.115,<1.0        # 웹 프레임워크 (SSE 스트리밍 엔드포인트)
uvicorn[standard]>=0.30,<1.0  # ASGI 서버 (--reload, 표준 확장 포함)
openai>=3.10,<4            # OpenAI 호환 SDK (docagent/llm.py, 1단계와 동일 범위)
python-dotenv>=1.0,<2      # .env 파일에서 환경 변수 로딩
