# 이 단계에서 실제로 쓰는 패키지만 나열한다.
# 버전은 범위로 쓴다(PROJECT-SPEC.md 10절 기준선, 2026-09-09 실측). 이전 단계에
# 이미 있던 패키지(openai, fastapi, uvicorn, python-dotenv, pydantic, httpx,
# pymilvus)는 같은 범위를 그대로 쓴다.
#
# pymilvus: BM25 Function(sparse 필드 자동 생성), AnnSearchRequest, RRFRanker,
# WeightedRanker, MilvusClient.hybrid_search API를 쓴다. VERIFIED-FINDINGS.md 3절:
# 이 API들은 pymilvus 2.6.17과 3.0.1에서 동일한 형태로 존재하는 것을 실제로
# 두 휠을 풀어 비교해 확인했다. Milvus Lite의 BM25 tokenizer 제약(standard/jieba만
# 지원)은 VERIFIED-FINDINGS.md 7절과 이 장 본문 2절에 적어 둔다.
pymilvus[milvus-lite]>=2.6,<3  # Milvus 클라이언트 + Milvus Lite(내장 모드, Windows 미지원)

openai>=3.10,<4                 # OpenAI 호환 SDK 호출(채팅·임베딩 공용, docagent/llm.py)

fastapi>=0.115,<1.0              # 웹 프레임워크
uvicorn[standard]>=0.30,<1.0     # ASGI 서버
python-dotenv>=1.0,<2            # .env 파일에서 환경 변수 로딩
pydantic>=2.7,<3                 # 요청 바디 검증(ChatRequest 등)
httpx>=0.28,<0.29                 # 테스트·스크립트에서의 보조 HTTP 호출

# 단위 테스트 실행용. 표준 라이브러리 unittest만으로도 tests/를 돌릴 수
# 있지만, pytest로 실행하면 출력이 더 읽기 편하다.
pytest>=8.0,<9
