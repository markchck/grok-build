# PROJECT-SPEC.md 10절: 범위로 쓰고, 각 줄에 이 단계에서 쓰는 이유를 남긴다.

fastapi>=0.115,<1.0        # SSE 대화·승인·재개 엔드포인트
uvicorn[standard]>=0.30,<1.0  # 로컬 실행 서버
pydantic>=2.7,<3           # 도구 인자 검증, 요청/응답 모델
httpx>=0.28,<0.29          # 모델 서버 호출(OpenAI 호환 REST)
python-dotenv>=1.0,<2      # .env 로딩

# 이 장은 pymilvus를 쓰지 않는다 — HITL과 무한루프 차단은 검색과 무관한
# 주제라서 4·5단계의 벡터 검색 의존성을 그대로 가져오지 않았다.
# 상태 저장은 표준 라이브러리 sqlite3만 쓴다(새 의존성 없음).

pytest>=8,<9                # 반복 감지·예산·승인 흐름 단위 테스트
