# 이 단계에서 실제로 쓰는 패키지만 나열한다.
# pymilvus 2.6.x 기준으로 코드를 작성했다. 2026-09 시점 PyPI에는 pymilvus 3.0.x도 올라와 있으나
# 이 장의 코드는 3.0.x API 변경 여부를 확인하지 못했다. 3.0.x를 쓰려면 먼저 공식 문서에서
# MilvusClient의 create_schema / add_field / prepare_index_params 시그니처가 같은지 확인한다.
pymilvus>=2.5,<3.0

# OpenAI 호환 모델 서버 호출(채팅·임베딩 공용)
openai>=1.40,<2.0

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
